-- https://www.lazyvim.org/configurations

return {
  "LazyVim/LazyVim",
  opts = {
    colorscheme = "vitesse",
    defaults = {
      keymaps = false,
    },
    icons = require("config.icons"),
  },
}
